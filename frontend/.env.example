# Public base URL of the astrology API (the backend/ service).
#
# Inlined into the bundle at build time, so it must be set before `next build`
# and a change needs a fresh deploy. Without it the app falls back to
# http://localhost:3001, which only works on your own machine.
NEXT_PUBLIC_API_URL=http://localhost:3001
